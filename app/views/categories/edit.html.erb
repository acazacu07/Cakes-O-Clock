<h1>Editing Category</h1>

<%= render 'form', category: @category %>

<div class="container margin">
	<a><%= link_to 'Show', @category, :class =>"btn-admin" %></a>
	<a><%= link_to 'Back', categories_path, :class =>"btn-admin" %> </a>
</div>
